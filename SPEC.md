# 軽貨物ドライバー「本当の手取り」計算ツール ― 実装仕様書 v1

この仕様書は、Claude Code / Cursor にそのまま渡して実装するためのものです。
目的は「電卓」ではなく、**入力→本当の手取りを見せて気づかせる→その場でアフィリエイト送客する**という"稼ぐ導線"です。

---

## 0. 全体像（このツールが何をするか）

1. ユーザーが自分の数字（売上・手数料・経費など）を入力する
2. 「本当の手取り」と「時給換算」を計算して大きく見せる
3. 経費の内訳を見せ、**一番重い項目**を強調する
4. その重い項目に対応する**アフィリエイトリンクを1〜2個だけ**提示する

計算はすべてブラウザ側（クライアントサイド）で完結。バックエンド不要。

---

## 1. 技術要件

- **形式**：1ページの Web アプリ（SPA）。ダウンロード不要でブラウザで動く。
- **推奨スタック**：素の HTML/CSS/JS でも可。React + Vite でも可（あなたの好みで）。フレームワーク必須ではない。
- **モバイルファースト必須**：利用者はドライバー＝スマホ。縦画面で片手操作できるUIにする。
- **バックエンド不要**：フォーム送信もサーバー保存もしない。入力値は画面内 state のみ（保存しない）。
- **依存を最小に**：軽く・速く。読み込み1秒以内を目標。
- アフィリエイトリンクは**設定用の定数オブジェクト**に集約し、あとから差し替えられるようにする（§5参照）。

---

## 2. 入力項目（フォーム）

すべて月額。各項目に「目安」をプレースホルダ表示し、初心者が埋めやすくする。

| id | ラベル | 型 | 初期値/目安 | 備考 |
|---|---|---|---|---|
| uriage | 月の売上（報酬総額） | 数値 | 400000 | 手数料を引く前の総額 |
| feeType | 手数料の種類 | 選択 | percent | percent / fixed を切替 |
| feeRate | 手数料率(%) | 数値 | 15 | feeType=percent のとき表示。相場15〜20% |
| feeFixed | 手数料(固定・月) | 数値 | 0 | feeType=fixed のとき表示。相場5,000〜20,000円 |
| gasoline | ガソリン代 | 数値 | 40000 | |
| vehicle | 車両費（リース/ローン） | 数値 | 30000 | |
| insurance | 保険（任意＋貨物） | 数値 | 20000 | |
| others | その他経費 | 数値 | 15000 | 駐車場・通信・備品・メンテ等 |
| workDays | 月の稼働日数 | 数値 | 22 | 時給換算に使用 |
| workHours | 1日の稼働時間 | 数値 | 10 | 時給換算に使用 |

- 全角数字が入力されても半角に変換して計算する（ドライバーはスマホ入力のため）。
- 未入力は 0 として扱い、計算は落とさない。

---

## 3. 計算ロジック（コア）

```
// 手数料額
feeAmount = (feeType === "percent") ? uriage * (feeRate/100) : feeFixed

// 経費合計
expenses = gasoline + vehicle + insurance + others

// 経費差引後（＝事業所得の目安・税引前）※これをメインの「手残り」とする
netBeforeTax = uriage - feeAmount - expenses

// 時給換算（メイン表示はこの税引前ベースで統一）
hourlyWage = netBeforeTax / (workDays * workHours)

// 売上に対する手残り率
keepRate = netBeforeTax / uriage * 100
```

### 3.1 税金モジュール（任意・v1.5で追加可）
税・国保・年金は自治体や控除で大きく変わり正確計算は重い。**v1では無理に入れず、「経費差引後」をメインにする**。
入れる場合も必ず「あくまで概算」と明記し、正確な額は会計ソフト/税理士へ、という送客フックにする（§4の会計ソフト導線に接続）。

概算を入れる場合の簡易式（すべて年額換算 = 月額×12 → 最後に月割り戻し）：
```
年事業所得 = netBeforeTax * 12
国民年金(概算) = 210000            // 固定・年額の目安
国保(概算)   = 年事業所得 * 0.10   // ざっくり率（自治体差大・要注記）
課税所得 = max(0, 年事業所得 - 国民年金 - 国保 - 480000(基礎控除) - (青色なら650000 else 0))
所得税(概算) = 課税所得 * 税率(簡易累進) - 控除額
住民税(概算) = 課税所得 * 0.10 + 5000
最終手取り(概算/月) = (年事業所得 - 国民年金 - 国保 - 所得税 - 住民税) / 12
```
※この数値は「概算です。正確な額は会計ソフトや税理士でご確認ください」と必ず添える。

---

## 4. 出力（結果表示）＝ここが稼ぎの心臓部

計算ボタンを押したら、**結果→内訳→出口リンク**の順で表示する。広告を先頭に出さない。

### 4.1 結果（大きく見せる）
```
あなたの手取り：月 XXX,XXX 円
時給換算：X,XXX 円
売上に対して手元に残るのは XX%
```
時給が「思ったより低い」という気づきが行動を生む。時給は太字で大きく。

### 4.2 内訳（重い項目を強調）
各項目を金額と「売上に対する％」で並べ、**最も大きいコスト項目を赤系で強調**し「ここが重い」と表示。
```
売上          400,000
− 手数料(15%)   60,000   ← 重い
− ガソリン      40,000
− 車両費        30,000   ← 重い
− 保険          20,000
− その他        15,000
─────────────
手残り         235,000
```

### 4.3 診断→出口リンク（1〜2個だけ）
内訳から「一番重い項目」を判定し、対応するCTAを**最大2個まで**出す。全部同時に出さない。

| 判定条件 | 出すCTA（カテゴリ） | 文言の主旨 |
|---|---|---|
| 手数料が最大 or 手数料率≧18% | 求人 | 「手数料の低い会社もあります。条件を比較（無料）」 |
| 時給 < 1,300円 | 求人 | 「今の条件、見直す価値。あなたに合う案件を探す（無料）」 |
| 車両費が最大 | カーリース | 「その車、リースの方が安いかも。無料で比較」 |
| 保険が最大 | 自動車保険 | 「保険は会社で数万円違うことも。無料一括見積もり」 |
| （税金の話題/常時サブ） | 会計ソフト | 「正確な税額と経費管理は会計ソフトで（無料体験）」 |
| （経費全般/サブ） | ガソリンカード | 「燃料費はカードで管理・節約」 |

判定は「最大コスト項目 → 1つ目のCTA」、「時給が低ければ求人を優先 → 2つ目」のように**主1・副1**で。

---

## 5. アフィリエイトリンクの設定（差し替え可能に）

リンクは1か所の設定オブジェクトに集約。A8の提携承認後に本URLを入れる。
**即時提携の3つ（オリコで乗ーる/ガソリンカード/ETC）は公開時にすぐ入る。他は審査通過後に差し替え。**

```js
const AFFILIATE = {
  // --- 即時提携：公開時にすぐ入れる ---
  lease_immediate:  { name: "オリコで乗ーる", url: "" },      // 契約30,000円
  gasoline:         { name: "法人ガソリンカード", url: "" },   // 5,000円 即時
  etc:              { name: "法人ETCカード", url: "" },        // 5,000円 即時

  // --- 審査通過後に差し替え・追加する本命 ---
  job_persol:       { name: "パーソル(ドライバー転職)", url: "" }, // 6,000円 EPC168 確定79%
  job_levjob:       { name: "レバジョブ", url: "" },              // 3,750円 EPC117 確定88%
  lease_niconori:   { name: "ニコノリ", url: "" },                // 審査通過17,500円
  insurance_insweb: { name: "インズウェブ(個人自動車保険)", url: "" }, // 見積1,714円 確定83%
  insurance_work:   { name: "はたらくクルマの自動車保険", url: "" },  // 見積1,000+申込2,000円
  accounting_yayoi: { name: "弥生シリーズ", url: "" },            // 申込300〜2,000円 確定98.5%
};
```

### 5.1 カテゴリ→表示するプログラムの対応
- 求人：`job_persol` を主、`job_levjob` を副
- リース：公開直後は `lease_immediate`（オリコ）、ニコノリ承認後は `lease_niconori` に差し替え
- 保険：`insurance_insweb` を主、事業寄りなら `insurance_work`
- 会計：`accounting_yayoi`
- 経費/燃料：`gasoline`（＋必要なら `etc`）

URLが空（未提携）のカテゴリは、CTAボタンを出さずスキップする（空リンクを踏ませない）。

---

## 6. 法令・A8規約まわり（必須）

- 画面のわかる位置に **「PR」または「広告を含みます」** と明記する。
- **断定表現を禁止**：「必ず安くなる」→「安くなる場合があります／〜かもしれません」。
- 税金表示は **「概算です。正確な額は会計ソフトや税理士でご確認ください」** を必ず併記。
- 別ページとして **運営者情報 / プライバシーポリシー / お問い合わせ** を用意（A8審査と信頼のため）。
- CTAは提案調で。誘導・煽りをしない。

---

## 7. 画面構成（1ページ）

```
[ヘッダー] タイトル + 「PR/広告を含みます」
[入力フォーム] §2の項目（モバイルで縦積み）
[計算ボタン] 大きく・親指で押せる位置
──（押下後、下に結果を表示）──
[結果] 手取り・時給・手残り率（大きく）
[内訳] 表 + 重い項目の強調
[診断コメント] 「あなたは〇〇の負担が重めです」
[出口CTA] 1〜2個（該当カテゴリのみ）
[注記] 概算である旨 + PR表記
[フッター] 運営者情報/プライバシー/問い合わせ へのリンク
```

---

## 8. 完成の定義（v1でここまで動けばOK）

- [ ] スマホで入力→計算→結果表示が滑らかに動く
- [ ] 手取り・時給・内訳が正しく出る（税金は入れなくてよい）
- [ ] 一番重い項目を判定し、対応CTAを1〜2個だけ出す
- [ ] AFFILIATE 設定を差し替えるだけでリンクが切り替わる
- [ ] PR表記・注記・運営者情報ページがある
- [ ] URL未設定カテゴリのCTAは自動で非表示になる

---

## 9. 公開後の流れ（このツールを作った後）

1. サーバー/ドメインに公開（Hostinger等。セルフバック対象）
2. 即時提携3件（オリコ/ガソリン/ETC）のURLを入れて走り出す
3. A8で本命（パーソル・レバジョブ・ニコノリ・インズウェブ・弥生）を提携申請
4. 承認され次第、AFFILIATE のURLを差し替え／追加
5. 配布：検索に頼らず、あなたがアクセスできる LINEオープンチャット・X・YouTube で「軽貨物専用の手取り計算ツール」として共有

---

## 10. 実装メモ（Claude Code / Cursor への指示のコツ）

- 「§3の計算式」「§4の出力仕様」「§5の AFFILIATE 構造」をそのまま貼って実装させると速い。
- まず計算＋結果表示だけ動かし（v1コア）、次に診断→CTA、最後に法令表記・運営者ページ、の順で作ると詰まらない。
- 税金モジュール（§3.1）は後回しでよい。まず「経費差引後の手取り＋時給」で公開する。
